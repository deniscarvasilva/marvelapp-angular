import { DeserializableModel } from './deserializable.model';

describe('Deserializable', () => {
  it('should create an instance', () => {
    expect(new DeserializableModel()).toBeTruthy();
  });
});
